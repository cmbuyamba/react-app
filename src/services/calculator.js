
const add = (a, b) => {
    return Number(a) + Number(b);
}

const multiply = (a, b) => {
    return Number(a) * Number(b);
}

const calculator = {
    add,
    multiply
}

export default calculator;